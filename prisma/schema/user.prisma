model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  address   String?
  street    String?
  age       Int?
  posts     Post[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
